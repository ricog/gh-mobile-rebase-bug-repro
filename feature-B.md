# Feature B

This file was added on `feature/example`. It is the PR's only change.

When the PR is updated via the **Update branch (Rebase)** option in the
GitHub Android mobile app, this commit should be replayed cleanly onto
the current `main` tip — yielding a single commit whose parent is the
`main` tip.

Instead, the mobile app produces a merge commit. See the README for full
details.
