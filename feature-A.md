# Feature A

Added to `main` after `feature/example` was created. Its purpose is only
to put `feature/example` one commit behind `main` so the PR offers the
**Update branch** action (with both merge-commit and rebase variants).

This file does not touch anything `feature/example` modifies, so the
rebase is guaranteed conflict-free.
